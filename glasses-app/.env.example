# Copy to .env.local and set to your deployed Worker URL (no trailing slash).
# For local Worker dev, use the http://<LAN-IP>:8787 address wrangler prints.
VITE_RELAY_URL=https://meater-relay.YOUR-SUBDOMAIN.workers.dev
